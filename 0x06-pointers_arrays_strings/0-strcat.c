#include "main.h"
#include <string.h>

/**
 * _strcat - prototype
 * @dest: dest
 * @scr: scr
 *
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{    

	strcat (dest, src);

return (dest);
}
