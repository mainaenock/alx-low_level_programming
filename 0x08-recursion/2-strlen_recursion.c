#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - function
 *
 * @s: string
 * Description: returns the length of the string
 * Return: int
 */

int _strlen_recursion(char *s)
{
	int i;

	i = strlen(s);

	return (i);
}
